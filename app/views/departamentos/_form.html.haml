= simple_form_for departamento, html: { class: 'form-horizontal'} do |f|
  = f.input :nombre
  = f.input :abreviatura
  .f-actions
    = f.button :submit, :class => "btn btn-primary"
    = link_to "Cancelar", departamentos_path, :class => "btn"